# Voy a sacar todos los numeros pares desde 0 hata 100
# usare el operador logico modulo, que al dividir entre 2 para hacer pares seria 0

for numero in range(0, 101):
    if numero % 2 == 0:
        print(numero)
